//both type and initial value

var empname: string = 'Jagan'
console.log(empname)
 
// only type
var empname: string
empname = 'Jagan'

//Only initialize
var empname = 'Jagan'

//without type and initialize
var empexp;

/*Var example : are available within the function. If declared outside function, then 
available everywhere Global variable
We can update or redefine a var variable
*/

/*Let keyword
Scope is only within block{}. If declared outside function, then available everywhere
we cannot update or redefine within same scope
*/

/*Const
should be declared and initialize at same time*/

